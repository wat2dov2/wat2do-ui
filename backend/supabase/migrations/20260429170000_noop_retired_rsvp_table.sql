-- Migration intentionally left as a no-op.
--
-- The RSVP table was retired before it became a supported frontend feature.
-- Keep this version file so previously applied Supabase migration versions
-- still line up, but do not create any table.

BEGIN;
COMMIT;
