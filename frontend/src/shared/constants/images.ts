/**
 * Shared image URLs and asset paths.
 *
 * Single source of truth for image constants used across features.
 */

/** Local path for the Mr. Goose mascot image (served from /public). */
export const GOOSE_IMAGE_PATH = "/images/mr-goose.png";
