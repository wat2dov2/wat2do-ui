/**
 * @deprecated Use useEventsStore directly from "@/features/events/store/events.store"
 * This hook is kept for backwards compatibility but all new code should use the store.
 */
export { useEventsStore as useEvents } from "@/features/events/store/events.store";
