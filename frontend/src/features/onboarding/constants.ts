/**
 * Onboarding feature constants.
 */

/** Duration (ms) for the faculty selection splash animation. */
export const SPLASH_ANIMATION_MS = 800;

/** Local path for the onboarding mascot image (served from /public). */
export const GOOSE_IMAGE_PATH = "/images/mr-goose.png";
