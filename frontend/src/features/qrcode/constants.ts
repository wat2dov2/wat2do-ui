/**
 * QR code feature constants.
 */

/** Canvas width and height (px) used when rendering a QR code to PNG. */
export const QR_CANVAS_SIZE = 512;

/** Maximum image upload size in bytes (5 MB). Matches the backend qr-assets bucket limit. */
export const MAX_UPLOAD_SIZE_BYTES = 5 * 1024 * 1024;
