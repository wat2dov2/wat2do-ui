/**
 * Backward-compatible re-export.
 * The canonical hook now lives in @/shared/hooks/usePostersFilters.
 */
export { usePostersFilters as useAdminPostersFilters } from "@/shared/hooks/usePostersFilters";
