# CLAUDE.md

@AGENTS.md

## Claude Code

`AGENTS.md` is the canonical repository policy. Load it before acting, follow it
for every non-trivial task, and resolve any conflict in favor of `AGENTS.md`.

Before code edits, provide the required pre-edit report from `AGENTS.md`:
- existing pattern matched,
- exact blast radius,
- scope-expanding decisions for confirmation.

